# Screenshots

Add real QQ/WeChat or local demo screenshots here before submission.

Required screenshots:

- command sent by the user;
- agent response with run directory;
- Green/Yellow/Red count summary;
- artifact folder with generated files;
- `citation_verification.csv` for success case;
- `citation_verification.csv` for boundary case;
- `tool_calls.jsonl`;
- `retrieved_literature.jsonl`.

Do not add fabricated chat screenshots or edited logs.
