# Failure Cases

No active generalization-audit failures after the Unicode rerun.

The original first-pass Windows console encoding failure is preserved in `unicode_rerun_note.md`, together with the fix and successful rerun evidence.
